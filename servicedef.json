{
  "launchType": "FARGATE",
  "desiredCount": 1,
  "networkMode": "awsvpc",
  "networkConfiguration": {
    "awsvpcConfiguration": {
      "subnets": [
        "${SUBNET_ONE}"
      ],
      "securityGroups": [
        "${SG_ONE}"
      ],
      "assignPublicIp": "ENABLED"
    }
  }
}